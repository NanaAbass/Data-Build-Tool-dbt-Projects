[project]
name = "pulsecare-analytics"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.12.0",
    "dbt-duckdb>=1.11.0",
]

[dependency-groups]
dev = [
    "duckdb>=1.5.5",
]
